import { Router } from "express";

const transactionRouter = Router();

// transactionRouter.post('',...); // making new transaction
// transactionRouter.get('/:transactionId',...); // transaction detail
// transactionRouter.post('/payments',...); // making payment
// transactionRouter.get('/payments/paymentId',...); // payment detail for the payment status

export default transactionRouter;